#include <chrono>
#ifndef INFO_H417_LENGTH_HPP
#define INFO_H417_LENGTH_HPP

#endif // INFO_H417_LENGTH_HPP

namespace tl {
class length{
    virtual ~length();
    virtual bool count();
    virtual bool output();
};
}